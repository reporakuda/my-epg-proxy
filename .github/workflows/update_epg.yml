name: Update My EPG

on:
  schedule:
    - cron: '23 */6 * * *' # Jalan setiap 6 jam
  workflow_dispatch:      # Tombol manual jika ingin update mendadak

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Update dari @v3 ke @v4
      - name: Checkout repo
        uses: actions/checkout@v4

      # Update dari @v4 ke @v5
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11' # Sekalian pakai Python yang lebih stabil/baru
      - name: Install dependencies
        run: pip install requests

      - name: Run grabber
        run: python grabber.py

      - name: Commit and Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add my_epg.xml
          git commit -m "Update EPG: $(date)" || exit 0
          git push
